name: Publish contort (Python) to PyPI

on:
  push:
    tags:
      - "py/contort-v*"

jobs:
  publish:
    uses: ./.github/workflows/py-shared-publish.yaml
    with:
      package: "./packages/contort/python"

permissions:
  id-token: write
  contents: read
