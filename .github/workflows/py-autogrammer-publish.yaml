name: Publish Autogrammer (Python) to PyPI

on:
  push:
    tags:
      - "py/autogrammer-v*"

jobs:
  publish:
    uses: ./.github/workflows/shared/python/publish.yaml
    with:
      package: "./packages/autogrammer/python"

permissions:
  id-token: write
  contents: read
